# PWA 테스트 가이드

PWA 기능이 성공적으로 설치되었습니다! 이제 테스트를 진행해보세요.

## 🚀 빠른 시작

### 1. 프로덕션 빌드 실행
```bash
npm run build
npm start
```

서버가 `http://localhost:3000`에서 실행됩니다.

## 🖥️ 데스크톱에서 테스트 (Chrome)

### PWA 설치 확인
1. Chrome에서 `http://localhost:3000` 접속
2. 주소창 오른쪽에 **설치 아이콘** (⊕) 표시 확인
3. 클릭하여 "글로벌픽 설치" 선택
4. 설치 후 독립 실행형 창으로 열림
5. 시작 메뉴/작업 표시줄에 앱 추가 확인

### Service Worker 확인
1. F12로 개발자 도구 열기
2. **Application** 탭 선택
3. **Service Workers** 섹션에서 활성화 확인
4. **Manifest** 섹션에서 앱 정보 확인 (이름, 아이콘, 테마 색상 등)

### 오프라인 기능 테스트
1. 개발자 도구 → **Network** 탭
2. **Offline** 체크박스 선택
3. 페이지 새로고침
4. 오프라인 페이지 표시 확인
5. 일부 캐시된 페이지는 여전히 작동해야 함

## 📱 모바일에서 테스트 (Android)

### 로컬 테스트 (같은 네트워크)
1. PC의 IP 주소 확인:
   ```bash
   ipconfig
   # IPv4 Address 찾기 (예: 192.168.0.10)
   ```

2. 스마트폰에서 `http://[PC의 IP]:3000` 접속

3. Chrome 메뉴 (⋮) → **홈 화면에 추가** 선택

4. 홈 화면에서 앱 아이콘 확인

### 온라인 테스트 (Vercel 배포 후)
1. Vercel에 배포
2. 스마트폰에서 앱 URL 접속
3. **홈 화면에 추가** 선택
4. 스플래시 스크린 표시 확인
5. 상태바 색상 확인 (파란색 #3b82f6)

## 🍎 iOS에서 테스트 (Safari)

### PWA 설치
1. Safari에서 앱 접속
2. 공유 버튼 (↑) 탭
3. **홈 화면에 추가** 선택
4. 앱 이름 확인 후 **추가**
5. 홈 화면에서 실행

### 주의사항
- iOS는 일부 PWA 기능에 제한이 있습니다
- 푸시 알림은 iOS 16.4+ Safari에서만 지원
- Service Worker의 캐시 용량 제한이 있음

## ✅ 체크리스트

### 기본 기능
- [ ] 주소창에 설치 아이콘 표시
- [ ] 앱 설치 가능
- [ ] 설치된 앱이 독립 실행형 창으로 열림
- [ ] 앱 아이콘이 정상적으로 표시
- [ ] 테마 색상이 적용됨 (파란색)

### Service Worker
- [ ] Service Worker가 등록됨
- [ ] Manifest 정보가 올바름
- [ ] 캐싱이 작동함

### 오프라인 기능
- [ ] 오프라인 페이지가 표시됨
- [ ] 캐시된 콘텐츠 접근 가능
- [ ] 온라인 복귀 시 자동 동기화

### 반응형 디자인
- [ ] 모바일에서 정상 작동
- [ ] 태블릿에서 정상 작동
- [ ] 데스크톱에서 정상 작동

## 🐛 문제 해결

### "설치 아이콘이 안 보여요"
- HTTPS 또는 localhost에서만 작동합니다
- Service Worker가 등록되었는지 확인
- Manifest 파일 경로 확인 (`/manifest.json`)

### "아이콘이 깨져 보여요"
- PNG 아이콘 파일을 생성해야 합니다
- `public/ICON_README.md` 파일 참고
- 임시로 아무 PNG 이미지를 사용해도 됩니다

### "오프라인이 작동 안 해요"
- 프로덕션 빌드(`npm run build`)에서만 작동
- 개발 모드(`npm run dev`)에서는 비활성화됨
- Service Worker 등록 확인

### "iOS에서 설치가 안 돼요"
- Safari 브라우저만 지원됩니다
- Chrome, Firefox 등 다른 브라우저는 불가
- "홈 화면에 추가" 옵션 사용

## 📊 성능 측정

### Lighthouse 점수 확인
1. Chrome 개발자 도구 → **Lighthouse** 탭
2. **Progressive Web App** 카테고리 선택
3. **Analyze page load** 클릭
4. PWA 점수 확인 (목표: 90점 이상)

### PWA 체크리스트
- Installable
- Works offline
- Uses HTTPS
- Has a manifest
- Has a service worker
- Fast and reliable

## 🎯 다음 단계

PWA 설치가 완료되었습니다! 이제 다음 기능을 추가할 수 있습니다:

1. **푸시 알림** - 새 리포트 발행 시 알림
2. **백그라운드 동기화** - 오프라인에서 작성한 메모 자동 동기화
3. **설치 프롬프트** - 사용자에게 설치 유도 배너
4. **업데이트 알림** - 새 버전 출시 시 알림

---

**설치 완료일**: 2026-04-17
**버전**: 1.0.0
**다음 검토일**: 배포 후 1주일
